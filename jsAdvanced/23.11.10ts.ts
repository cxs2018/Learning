let a: number = 6;
let list: number[] = [1, 2, 3];
let list2: Array<number> = [1, 2, 3];
let x: [string, number, object] = ["1", 2, {}];
enum Color {
  Red = 1,
  Green,
  Blue,
}

let c: Color = Color.Red;
let colorName: string = Color[1];
